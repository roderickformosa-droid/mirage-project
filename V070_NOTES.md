# v0.70 Field Hotfix

- Added a hard per-frame exception guard so a bad OpenCV/AI frame is skipped instead of terminating the app process.
- Compact field UI: smaller mode/stage/evidence text and thinner progress bar.
- Removed AZ/ANGLE/RANGE HUD from the live observation area.
- Result arrow reduced in size and only appears for a published result.
- While scanning, the central status shows the current stage instead of a large NO RELIABLE WIND CONDITION block.
- Control strip reduced to preserve more camera preview area.
- All v0.69 adaptive observer logic retained.
